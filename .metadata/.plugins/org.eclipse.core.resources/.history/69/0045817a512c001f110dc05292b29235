package Modelo;

import conexionBase.conexionBD;
import java.sql.*;
public class desplegarPorCategoria {
	public String[] datos(int categoria) {
		String consulta= "SELECT ID_producto, nombre, fecha_vencimiento, stock, precio_venta from productos WHERE categoria="+categoria+";";
		conexionBD conec= new conexionBD();
		Connection conn= conec.conexion();
		PreparedStatement ps= null;
		ResultSet rs= null;
		try {
			
		}catch(Exception e) {
			
		}
	}
}
